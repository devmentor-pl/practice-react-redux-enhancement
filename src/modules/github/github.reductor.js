const reducer = (state = initialState, { type, payload }) => {
    switch (type) {
        case 'CASE':
            return;
        case 'CASE':
            return;
        default:
            return state;
    }
};

export default reducer;
